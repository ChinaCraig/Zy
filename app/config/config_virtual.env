# 虚拟人配置文件
# 此文件包含虚拟人交互相关的配置参数

# 虚拟人服务URL
# 虚拟人服务的连接地址
VIRTUAL_HUMAN_SERVICE_URL=http://localhost:9000/virtual

# 虚拟人服务API密钥
# 用于访问虚拟人服务的身份验证密钥
VIRTUAL_HUMAN_API_KEY=your_virtual_human_api_key

# 默认虚拟人
# 当用户未指定虚拟人时使用的默认虚拟人ID
DEFAULT_VIRTUAL_HUMAN=xiaojia

# 可用的虚拟人动作列表
# 目前只有一个转圈动作，未来可扩展更多动作
# 格式：动作名称=动作代码
VIRTUAL_HUMAN_ACTIONS=spin=1,stop=0

# 虚拟人动作对应的中文指令识别
# 格式：中文指令=动作名称
# 转圈动作的识别词
SPIN_COMMANDS=转圈,旋转,转动,开始转圈,转起来,旋转起来
# 停止动作的识别词
STOP_COMMANDS=停止,停下,别转了,停止转圈,不要转了,站好

# 虚拟人表情配置
# 可用的表情列表
VIRTUAL_HUMAN_EMOTIONS=happy,sad,angry,surprised,neutral

# 虚拟人配置参数
# 3D模型路径
VIRTUAL_HUMAN_MODEL_PATH=/models/virtual_human_default.glb

# 虚拟人渲染设置
VIRTUAL_HUMAN_RENDER_QUALITY=high
VIRTUAL_HUMAN_SHADOW_ENABLED=true

# 虚拟人音频设置
VIRTUAL_HUMAN_VOICE_ENABLED=true
VIRTUAL_HUMAN_VOICE_TYPE=female_zh

# 虚拟人响应模型
# 用于生成虚拟人响应内容的模型
VIRTUAL_HUMAN_RESPONSE_MODEL=gpt-3.5-turbo

# 虚拟人行为分析模型
# 用于分析用户指令并转换为虚拟人行为的模型
VIRTUAL_HUMAN_ACTION_MODEL=gpt-4

# 调试模式
VIRTUAL_HUMAN_DEBUG=false 